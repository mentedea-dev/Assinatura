/**
 * Signature image assets are served via the tRPC `assets.signatureImages` endpoint.
 * These constants are kept for backward compatibility with server-side code only.
 * The frontend loads assets dynamically via tRPC to avoid bundling large base64 strings.
 */
export const SIG_SYMBOL_B64 = "";
export const SIG_WORDMARK_B64 = "";
